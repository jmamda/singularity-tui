# Force LF everywhere so Windows checkouts match prettier's endOfLine: lf
# (GitHub's Windows runners default to autocrlf, which fails `prettier --check`).
* text=auto eol=lf
*.png binary
*.jpg binary
*.gif binary
*.ico binary
*.tgz binary
